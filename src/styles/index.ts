import { accordionDetailsClasses } from "@mui/material";

export const styles = {
    accordion: {
        marginTop: '12px',
        [`& .${accordionDetailsClasses.root}`]: {
            padding: 0
        }
    
    }
};